#calculator <- function(start,end) {
#  (100*abs(start-end)/end)
#}
#
#calculator(1.2935,1.237)

ahh <- function(name, text, age){
  age 
  paste(name,"is", age, "and", text)
}
ahh(name <- "Ayomide", text <- "should be ambitious", age <- (2**79))
